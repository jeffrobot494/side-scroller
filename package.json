{
  "name": "side-scroller",
  "private": true,
  "type": "module",
  "description": "Static, no-build browser game. package.json carries no dependencies: it exists so node runs the ESM test suite, and so a host can find `npm start`. The browser ignores it and there is still no build step.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node test/run.mjs"
  }
}
