---
type: idea
category: content-generation
resolution: vague
---

# Idea: advanced level generation

Things we might do to level generation later. Nothing here is agreed or
scheduled. What is built: `tech/level-generation.md`.

## Parked

| | |
|---|---|
| Separate jump ability from gap spans | Level generation must not read the player's jump distance. The two may be related, but they need to be distinct numbers — minor jump tweaks should not reshape levels, multiple characters have different jump distances, and levels need to be able to generate impossible spans |
