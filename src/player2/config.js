// Player2 game identity. Safe to ship in the client: the game client id is a
// public identifier (like an OAuth client_id), not a secret. The per-user p2Key
// is fetched at runtime from the local Player2 app and never stored.
export const GAME_CLIENT_ID = "019f8327-10a8-7de8-a009-40f10fef0763";
